import { NavBar } from 'components/NavBar/NavBar';
import { HeaderWrapper } from './Header.styled';

export const Header = () => {
  return (
    <HeaderWrapper>
      <NavBar />
    </HeaderWrapper>
  );
};
